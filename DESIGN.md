# มาตรฐานหน้าตา Cielcard

กติกากลางของหน้าตาแอป งานออกแบบทุกชิ้นหลังจากนี้ยึดตามไฟล์นี้ ถ้าจะทำต่างจากนี้ต้องมีเหตุผลและเขียนกำกับไว้

เป้าหมาย: **เรียบ อ่านง่าย ใช้ได้จริงในร้าน** — คนใช้งานยืนหน้าเคาน์เตอร์ กดบนมือถือ ต้องเห็นปุ๊บรู้ปั๊บว่ากดอะไร

---

## 1. สเกลที่ใช้ได้ (ห้ามคิดเลขใหม่เอง)

| อย่าง | ค่าที่ใช้ได้ |
|---|---|
| ระยะห่าง | `4 · 8 · 12 · 16 · 24 · 32px` |
| ตัวอักษร | `.72rem` (กำกับ) · `.78rem` (ป้าย/ปุ่มในตาราง) · `.86rem` (เนื้อหา) · `.95rem` (เมนู) · `1.1rem` (หัวข้อ) |
| มุมโค้ง | `9px` (ปุ่ม/ช่องกรอก) · `13px` (กล่องเล็ก) · `20px` (popup) · `999px` (ป้าย/ชิป) |

ก่อนเพิ่มค่าใหม่ ให้ถามก่อนว่ามีค่าเดิมที่ใกล้เคียงใช้แทนได้ไหม — เกือบทุกครั้งมี

## 2. สี = ความหมาย ห้ามใช้เพื่อความสวย

ประกาศไว้ที่ `:root` ที่เดียว ห้ามเขียนโค้ดสีตรง ๆ ในกฎย่อย

| Token | ใช้กับ |
|---|---|
| `--t-ok-*` 🟢 | สำเร็จ · ขายแล้ว · มีของ |
| `--t-wait-*` 🟡 | รอ · ค้างอยู่ · รอขาย |
| `--t-info-*` 🔵 | ข้อมูลทั่วไป · สะสม |
| `--t-warn-*` 🟠 | ต้องระวัง · รออนุมัติ |
| `--t-bad-*` 🔴 | ยกเลิก · ลบ · อันตราย |
| `--t-alt-*` 🟣 | หมวดพิเศษ เช่น ร้าน / ชุดรวม |

**ทำไมต้องผ่าน token:** โหมดมืดแก้แค่ `--t-*-fg` ที่เดียวจบ ก่อนหน้านี้มีสีเขียนตายไว้ 29 จุด ซึ่งอ่านไม่ออกบนพื้นมืดทั้งหมด

## 3. ปุ่มกับป้าย

- ป้าย (`.badge`) · ชิป (`.act-chip`) · ปุ่มในตาราง (`.btn-act`) **สูงเท่ากัน 28px ตัวอักษร .78rem** เสมอ — คอลัมน์ที่อยู่ติดกันจะได้ไม่เบี้ยว
- 1 แถวมี **ปุ่มหลัก (สีฟ้า) ได้ปุ่มเดียว** ที่เหลือเป็นปุ่มโปร่ง
- ปุ่มในตารางกว้างขั้นต่ำ 78px เท่ากันหมด ไม่ยืดหดตามความยาวคำ
- **ห้ามห้อยข้อความใต้ปุ่ม** รายละเอียดยาว → tooltip หรือไปบอกใน popup ตอนกด

## 4. ตาราง

- คอลัมน์ที่เนื้อหาสั้นและอยู่ติดกัน (สถานะ / กระทำ) **กว้างเท่ากัน** และจัดกึ่งกลางทั้งคู่
- 1 ช่อง = 1 เรื่อง ไม่ยัดหลายอย่าง
- ตัวเลขชิดขวา ข้อความชิดซ้าย สถานะกึ่งกลาง
- หัวตารางกับ colspan ของแถวคั่นต้องเท่ากันเสมอ (มีเทสต์คุมไว้)

## 5. Popup

- อยู่**กลางจอ**เสมอ เนื้อหายาวเกินจอให้เลื่อนในกล่อง ไม่ดันหัวกล่องหลุดขอบ
- โครง: หัวข้อ 1 บรรทัด → รายละเอียด → (ตัวเลขสำคัญ ถ้ามี) → ปุ่ม
- ปุ่ม: **ยกเลิกซ้าย · ยืนยันขวา** คำบนปุ่มบอกสิ่งที่จะเกิดขึ้น ("ยืนยันคืนสินค้า" ไม่ใช่ "ตกลง")
- **ปิดทิ้ง (คลิกนอกกล่อง / Esc) = ไม่ได้ตัดสินใจอะไร** ห้ามนับเป็นการกดยกเลิก ถ้าการยกเลิกมีผลจริง

## 6. หัวข้อหน้า

- 1 หน้ามีหัวข้อเดียว ถ้ามีแถบหัวด้านบนแล้ว ห้ามมี `h2` ซ้ำอีก
- แถบหัวยาวเต็มความกว้าง ปุ่มเครื่องมือ (เช่น สลับโหมด) อยู่ขวาสุด

## 7. โหมดสว่าง/มืด

- ทุกอย่างต้องอ่านออกทั้งสองโหมด **เช็คทั้งคู่ก่อน deploy เสมอ**
- โหมดมืด = น้ำเงินค่อนไปทางดำ (`#17294a → #0d1930 → #080f1e`)
- ผู้ใช้เลือกเองได้ทุกบัญชี เก็บที่ `app_theme`

## 8. คำที่ใช้เรียกสถานะ (ใช้ชุดเดียวทั้งแอป)

`สะสม` · `รอขาย` · `ขายแล้ว` · `มีของ` / `ไม่มีของ` · `รอดำเนินการ` · `สำเร็จ` · `ยกเลิก`

ห้ามมีคำพ้องความหมายซ้อน เช่น "ของไม่มี" กับ "ไม่มีของ" ต้องเลือกอันเดียว

## 9. ตอนรอเซิร์ฟเวอร์

- **ห้ามใช้ตัวหนังสือ "กำลัง..." เป็นตัวบอกสถานะหลัก** ใช้ตัวหมุนแทน
- ตัวนับงานอยู่ที่ `fetch` ที่เดียว (`busyStart`/`busyEnd`) — **ทุกอย่างที่ยิงเซิร์ฟเวอร์ได้ตัวหมุนอัตโนมัติ ไม่ต้องไล่ใส่เอง** ของที่เขียนเพิ่มทีหลังก็ได้ไปด้วย
- ป้ายหมุนขึ้นเมื่องานช้ากว่า 250ms เท่านั้น (เร็วกว่านั้นจะกะพริบถี่จนน่ารำคาญ)
- ปุ่มที่กดแล้วต้องรอ ให้ครอบด้วย `runBusy(this, fn)` — ปุ่มหมุนในตัวเองและกดซ้ำไม่ได้
- ข้อความผลลัพธ์: สำเร็จ = จางหายเองใน 2.5 วิ · ผิดพลาด = ค้างไว้ให้เห็น ห้ามหายเงียบ

## 10. ก่อน deploy ทุกครั้ง

1. `node test-activity.mjs && node test-return.mjs && node test-merge.mjs && node test-theme.mjs && node test-busy.mjs && node test-rows.mjs`
2. เปิดดูทั้งโหมดสว่างและมืด
3. `git push` แล้ว **ต้อง** `vercel --prod --yes` และ `vercel alias set <url> cielcard.vercel.app`
4. เทียบ md5 ของไฟล์บนเว็บกับในเครื่องว่าตรงกัน
